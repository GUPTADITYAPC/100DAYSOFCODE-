GEEKS FOR GEEKS
Minimum Steps Required
CODE:
--------------------------------------------------------
int minSteps(string str) {
    // Write your code here.
    int count=0;
    for( int i=1;i<str.length();i++)
    if(str[i]!=str[i-1])
    count++;
    if(count%2==0)
    return count/2+1;
    else return count/2+2;
}
