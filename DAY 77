GEEKS FOR GEEKS 
Binary Modulo
CODE:
--------------------------------------------------
    int modulo(string s,int m)
        {
            //code here
            int k=0;
            for( int i=0;i<s.length();i++)
            {
                k=k*2+(s[i]-'0');
                k%=m;
            }
            return k;
        }
